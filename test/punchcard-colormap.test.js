var test = require('tape').test;
var PunchcardColorMap = require('./../../build/js/punchcard.js');

console.log(PunchcardColorMap);


test('The add method...', function(t) {

    // var actual,
    //     expected;
    //
    // actual = add(1, 2);
    // expected = 4;

    t.pass('dummy test');

    // notify tape that there are no more tests
    t.end();

})

